[package]
name = "learn-to-compile"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.145"
pyo3 = { version = "0.23", features = ["auto-initialize"] } # Embeds a python interpreter in the rust code? kinda gross...
